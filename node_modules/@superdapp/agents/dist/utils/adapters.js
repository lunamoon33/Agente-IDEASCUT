"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isNodeJS = exports.isCloudflareWorkers = void 0;
exports.createHttpsAgent = createHttpsAgent;
exports.log = log;
// Environment detection
exports.isCloudflareWorkers = typeof globalThis !== 'undefined' && 'caches' in globalThis;
exports.isNodeJS = typeof process !== 'undefined' && process.versions && process.versions.node;
// HTTPS Agent adapter
function createHttpsAgent() {
    if (exports.isNodeJS) {
        try {
            const https = require('node:https');
            return new https.Agent({
                rejectUnauthorized: false,
            });
        }
        catch (error) {
            console.warn('HTTPS module not available');
            return { rejectUnauthorized: false };
        }
    }
    return { rejectUnauthorized: false };
}
// Environment-specific console logging
function log(message, level = 'log') {
    if (exports.isCloudflareWorkers) {
        // In Cloudflare Workers, console.log might not be available in all contexts
        try {
            console[level](message);
        }
        catch (error) {
            // Fallback for environments where console is not available
            // You might want to implement a different logging strategy here
        }
    }
    else {
        console[level](message);
    }
}
//# sourceMappingURL=adapters.js.map