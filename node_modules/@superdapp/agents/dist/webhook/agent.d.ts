import { Message } from '../types';
export type RequestHandler = (event: Message) => Promise<void>;
export declare class WebhookAgent {
    private registry;
    constructor();
    addCommand(command: string, handler: RequestHandler): void;
    addCommands(commands: Record<string, RequestHandler>): void;
    onMessage(handler: RequestHandler): void;
    processRequest(body: Message): Promise<void>;
}
//# sourceMappingURL=agent.d.ts.map