import { SuperDappClient } from './client';
import { BotConfig, CommandHandler, AgentCommands, AgentMessages, ReplyMarkupAction } from '../types';
export interface SuperDappAgentOptions {
    secret?: string | undefined;
}
export declare class SuperDappAgent {
    private webhookAgent;
    private client;
    private commands;
    private messages;
    constructor(config: BotConfig);
    /**
     * Add a command handler
     */
    addCommand(command: string, handler: CommandHandler): void;
    /**
     * Add multiple commands at once
     */
    addCommands(commands: AgentCommands): void;
    /**
     * Add message templates
     */
    addMessages(messages: AgentMessages): void;
    /**
     * Process a webhook request body
     */
    processRequest(body: unknown): Promise<void>;
    /**
     * Send a message to a connection (DM)
     */
    sendConnectionMessage(roomId: string, message: string, options?: {
        isSilent?: boolean;
    }): Promise<import("../types").ApiResponse<unknown>>;
    /**
     * Send a message to a channel
     */
    sendChannelMessage(channelId: string, message: string, options?: {
        isSilent?: boolean;
    }): Promise<import("../types").ApiResponse<unknown>>;
    /**
     * Send a message with reply markup
     */
    sendReplyMarkupMessage(type: 'buttons' | 'multiselect', roomId: string, message: string, replyMarkup: ReplyMarkupAction[][], options?: {
        isSilent?: boolean;
    }): Promise<import("../types").ApiResponse<unknown>>;
    /**
     * Get the underlying client for advanced operations
     */
    getClient(): SuperDappClient;
    private createCommandWrapper;
    getCommands(): string[];
    private handleMessage;
    private handleCallbackQuery;
    private parseMessage;
    private getRoomId;
    private isCallbackQuery;
}
//# sourceMappingURL=agent.d.ts.map