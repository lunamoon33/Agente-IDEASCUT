import { ApiResponse, BotConfig, ReplyMarkup, SendMessageOptions, BotInfoResponse } from '../types';
export declare class SuperDappClient {
    private axios;
    private config;
    constructor(config: BotConfig);
    private setupInterceptors;
    /**
     * Send a message to a channel
     */
    sendChannelMessage(channelId: string, options: SendMessageOptions): Promise<ApiResponse>;
    /**
     * Send a message to a connection (DM)
     */
    sendConnectionMessage(roomId: string, options: SendMessageOptions): Promise<ApiResponse>;
    /**
     * Send a message with reply markup (buttons, multiselect, etc.)
     */
    sendMessageWithReplyMarkup(roomId: string, message: string, replyMarkup: ReplyMarkup, options?: {
        isSilent?: boolean;
    }): Promise<ApiResponse>;
    /**
     * Send a message with button actions
     */
    sendMessageWithButtons(roomId: string, message: string, buttons: Array<{
        text: string;
        callback_data: string;
    }>, options?: {
        isSilent?: boolean;
    }): Promise<ApiResponse>;
    /**
     * Join a social group/channel
     */
    joinChannel(channelNameOrId: string, messageId?: string): Promise<ApiResponse>;
    /**
     * Leave a social group/channel
     */
    leaveChannel(channelNameOrId: string, messageId?: string): Promise<ApiResponse>;
    /** Get user channels list */
    getChannels(userId: string): Promise<ApiResponse>;
    /**
     * Get bot channels list
     */
    getBotChannels(): Promise<ApiResponse>;
    /**
     * Get info about the authenticated bot
     */
    getBotInfo(): Promise<ApiResponse<BotInfoResponse>>;
    /**
     * Alias for getBotInfo (compatibilidade)
     */
    getMe(): Promise<ApiResponse<BotInfoResponse>>;
    /**
     * Update a direct message in a connection (DM)
     * Accepts a string or an object with { body }
     */
    updateConnectionMessage(connectionId: string, messageId: string, message: string | {
        body: string;
    }): Promise<ApiResponse>;
    /** Delete a direct message in a connection (DM) */
    deleteConnectionMessage(connectionId: string, messageId: string): Promise<ApiResponse>;
    /**
     * Update a message in a channel
     * Accepts a string or an object with { body }
     */
    updateChannelMessage(channelId: string, messageId: string, message: string | {
        body: string;
    }): Promise<ApiResponse>;
    /** Delete a message in a channel */
    deleteChannelMessage(channelId: string, messageId: string): Promise<ApiResponse>;
}
//# sourceMappingURL=client.d.ts.map