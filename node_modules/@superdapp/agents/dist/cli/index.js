#!/usr/bin/env node
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const commander_1 = require("commander");
const chalk_1 = __importDefault(require("chalk"));
const create_1 = require("./commands/create");
const configure_1 = require("./commands/configure");
const run_1 = require("./commands/run");
const status_1 = require("./commands/status");
const program = new commander_1.Command();
program
    .name('superagent')
    .description('SuperDapp AI Agents SDK and CLI')
    .version('1.0.0');
// Add commands
program.addCommand(new create_1.CreateCommand());
program.addCommand(new configure_1.ConfigureCommand());
program.addCommand(new run_1.RunCommand());
program.addCommand(new status_1.StatusCommand());
// Global error handler
process.on('uncaughtException', (error) => {
    console.error(chalk_1.default.red('Uncaught Exception:'), error);
    process.exit(1);
});
process.on('unhandledRejection', (reason) => {
    console.error(chalk_1.default.red('Unhandled Rejection:'), reason);
    process.exit(1);
});
(async () => {
    try {
        // Parse command line arguments and await async actions
        await program.parseAsync();
        // Show help if no command provided
        if (!process.argv.slice(2).length) {
            program.outputHelp();
        }
    }
    catch (err) {
        console.error(chalk_1.default.red('CLI Error:'), err);
        process.exit(1);
    }
})();
//# sourceMappingURL=index.js.map